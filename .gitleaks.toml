title = "Trovaya gitleaks config"

# Historic dummy buyer key committed in 6b2a98a (now replaced by TEST_BUYER_PRIVATE_KEY env).
# Allowlist prevents CI failure when scanning first-parent range that still contains that commit.
# New code no longer contains the literal – keep this allowlist narrow.

[allowlist]
description = "test vault dummy buyer key – unfunded test vector, now env-only"
paths = [
  '''packages/contracts/scripts/test-vault-delivery\.ts''',
  '''scripts/test-vault-delivery\.mjs''',
]
regexes = [
  '''0x532b61459b0d6e77b7d8be8f756a959b9b6898e82625c30276b7ebdf9c4deead''',
]
