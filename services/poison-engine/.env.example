TROVAYA_ALLOWED_ORIGINS=http://localhost:3000
# Optional when Python is not available on PATH:
TROVAYA_PYTHON=
# Limiter is process-local. Use one worker for the demo; multi-instance deployment requires Redis.
TROVAYA_UVICORN_WORKERS=1
